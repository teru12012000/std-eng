import { style } from '@vanilla-extract/css'

const styles = {
    containar: style({
        width: '75%',
        margin: '50px auto',
    }),
    titleBox: style({
        fontSize: '40px',
        fontWeight: 'bold',
        textAlign: 'center',
    }),
}

export default styles
